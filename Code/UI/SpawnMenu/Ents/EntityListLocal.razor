@using Sandbox;
@using Sandbox.UI;
@inherits Panel
@namespace Sandbox


<VirtualGrid Items=@Entities ItemSize=@(160)>
    <Item Context="item">
        @if (item is ScriptedEntity entry)
        {
            <SpawnMenuIcon Ident=@($"entity:{entry.ResourcePath}") Title=@entry.Title></SpawnMenuIcon>
        }
    </Item>
</VirtualGrid>

@code
{
    List<ScriptedEntity> Entities = [];

    protected override void OnParametersSet()
    {
        base.OnParametersSet();

        Entities = ResourceLibrary.GetAll<ScriptedEntity>().ToList();


    }
}
